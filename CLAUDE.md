# 仓库与写作规则

## 内容边界

- `docs/` 只保存会由 MkDocs 发布的公开站点内容。
- `management/` 保存站点方向、迁移审核、来源清单等内部管理记录，不加入 MkDocs 导航或搜索。
- `README.md` 面向仓库使用者，说明项目用途、预览方式和基本写作流程，不承载 Agent 规则。

## 文章结构

- 文章统一放在 `docs/articles/`，并按内容分类组织；技术与非技术文章都遵守这一规则。
- 日期通过 frontmatter 管理，不使用年份、月份或日期作为文章源码目录。
- 每篇文章可以使用独立目录共同存放正文与附件。正文文件名沿用现有约定，与文章目录同名。
- 不为已删除的日期目录维护跳转页；不要仅为改变源码布局而引入重定向。

## 维护约定

- 结构调整应同步更新 `management/` 中相关决策或迁移记录。
- 管理记录应保留历史事实、来源和审核结论，不把历史描述当作当前规则。
- 新增长期规则时修改本文件；`AGENTS.md` 仅作为兼容入口。
- 修改公开内容或站点配置后，运行 `python -m mkdocs build --strict`。
